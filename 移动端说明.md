# 移动端功能说明

## 新增功能

### 1. PWA 支持（可安装应用）

应用现在支持 PWA（渐进式 Web 应用），可以安装到设备桌面：

**Android (Chrome)**
- 使用 Chrome 打开应用
- 点击右上角菜单 → "添加到主屏幕"
- 或者看到提示时点击"安装应用"

**iOS (Safari)**
- 使用 Safari 打开应用
- 点击底部分享按钮
- 选择"添加到主屏幕"

**桌面端 (Chrome/Edge)**
- 地址栏右侧会出现安装图标
- 点击即可安装为独立应用

### 2. 移动端 UI 优化

- **响应式布局**: 支持手机、平板、桌面各种屏幕尺寸
- **触摸优化**: 按钮和卡片尺寸针对触摸操作调整
- **安全区域适配**: 支持 iPhone 刘海屏安全区域
- **禁用缩放**: 防止误触缩放，提供更原生体验

### 3. 移动端 Meta 标签

```html
- 禁用用户缩放
- 支持全屏显示
- Apple 设备专用图标
- 主题颜色适配状态栏
```

## 构建说明

### 移动端部署

```bash
# 开发模式
npm run dev

# 生产构建
npm run build

# 预览构建结果
npm run preview -- --host 0.0.0.0
```

### 图标生成

如需更换应用图标，将图标文件放在 `public/` 目录下：
- `icon-192.png` - 192x192 像素
- `icon-512.png` - 512x512 像素

## 注意事项

1. **自动备份功能**: File System Access API 在移动端浏览器可能不支持，建议使用数据导出/导入功能
2. **离线使用**: PWA 安装后可离线使用，但首次使用需要网络连接
3. **数据持久化**: 数据保存在浏览器本地存储中，清除浏览器数据会丢失

## 版本信息

- 项目版本：0.1.0
- 新增：PWA 支持、移动端优化
- 更新日期：2026-03-23
