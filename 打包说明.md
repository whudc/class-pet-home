# 班级宠物园 - 打包说明

## 打包结果

✅ **打包成功完成！**

- **输出目录**: `dist/`
- **文件总数**: 625 个文件
- **总大小**: 约 34MB
- **入口文件**: `dist/index.html`

## 目录结构

```
dist/
├── index.html              # 主页面入口
└── assets/                 # 静态资源目录
    ├── index-*.js          # 打包后的 JavaScript
    ├── index-*.css         # 打包后的样式表
    └── *.webp              # 宠物图片资源（10 种宠物 × 10 个成长阶段）
```

## 在希沃白板上使用

### 方法一：直接拷贝 dist 目录（推荐）

1. 将整个 `dist` 文件夹拷贝到希沃白板的存储位置
2. 使用希沃白板的浏览器打开 `index.html` 文件
3. 即可正常使用

### 方法二：使用简单 HTTP 服务器

如果希沃白板支持运行简单的 HTTP 服务器：

**使用 Python（如已安装）**：
```bash
# 进入 dist 目录
cd dist

# 启动 HTTP 服务器
python -m http.server 8080
```

然后在希沃白板浏览器中访问：`http://localhost:8080`

### 方法三：使用 preview 命令预览

```bash
npm run preview -- --host 0.0.0.0 --port 4173
```

然后在希沃白板浏览器中访问：`http://设备 IP:4173`

## 注意事项

1. **无需安装 Node.js** - 打包后的 `dist` 目录是纯静态文件，可以直接在浏览器中运行，不需要安装任何依赖

2. **数据保存** - 应用数据保存在浏览器 localStorage 中，建议：
   - 定期使用"设置与帮助 → 数据管理 → 下载导出"功能备份数据
   - 或开启自动备份功能（需要浏览器支持 File System Access API）

3. **浏览器兼容性** - 需要现代浏览器支持（Chrome/Edge 推荐）
   - 希沃白板内置浏览器通常基于 Chrome，可以直接使用
   - 如遇问题，请尝试更新希沃白板到最新版本

4. **离线使用** - 打包后的应用可以完全离线使用，无需联网

## 快速测试

在本地测试打包结果：

```bash
npm run preview
```

然后在浏览器中访问显示的地址即可预览。

## 重新打包

如需重新打包（例如修改了代码），执行：

```bash
npm run build
```

新的构建结果会覆盖 `dist` 目录。

## 文件清单

### 主要文件
- `dist/index.html` - 主页面（403 字节）

### 资源文件
- `dist/assets/index-*.js` - 主应用脚本
- `dist/assets/index-*.css` - 主样式表
- `dist/assets/*.webp` - 宠物图片（约 600+ 张）

### 宠物种类
项目包含 10 种宠物，每种宠物 10 个成长阶段：
- 三花猫
- 中华田园犬
- 乌龟
- 九尾狐
- 仓鼠
- 加菲猫
- 卡皮巴拉（水豚）
- 变色龙
- 吉娃娃
- 哈士奇
- （以及其他宠物...）

## 技术支持

如有疑问，请联系原作者：
- 微信：duan cong
- 暗号：班级宠物园

---

**打包时间**: 2026-03-23
**Vite 版本**: 6.4.1
**Node.js 版本**: 20.10.0
