# 班级宠物园（Class Pet Home）项目介绍

## 一、项目概述

**班级宠物园**是一款面向课堂管理的轻量级 Web 应用，采用 **Vue 3 + TypeScript + Vite + Tailwind CSS** 技术栈构建。项目通过将学生积分与"宠物养成"机制结合，实现趣味化的课堂行为管理。

### 核心功能
- 学生评价系统（加分/扣分）
- 宠物养成系统（升级、徽章）
- 小商店兑换系统
- 多维度排行榜
- 数据导入/导出与自动备份

### 技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.5.13 | 响应式框架 |
| Pinia | 2.3.1 | 状态管理 |
| TypeScript | 5.8.2 | 类型支持 |
| Vite | 6.2.2 | 构建工具 |
| Tailwind CSS | 3.4.17 | 样式框架 |

---

## 二、项目目录结构

```
class-pet-home-local/
├── src/                          # 源码目录
│   ├── assets/                   # 静态资源
│   │   ├── pets/                 # 宠物图片（10 种宠物×10 个成长阶段）
│   │   └── tailwind.css          # Tailwind 样式入口
│   ├── components/               # Vue 组件
│   │   ├── modals/               # 弹窗组件
│   │   │   ├── ModalAdoptPet.vue     # 领养宠物弹窗
│   │   │   ├── ModalBase.vue         # 弹窗基础容器
│   │   │   ├── ModalClassManager.vue # 班级管理弹窗
│   │   │   ├── ModalLeaderboard.vue  # 排行榜弹窗
│   │   │   ├── ModalRecords.vue      # 评价记录弹窗
│   │   │   ├── ModalScore.vue        # 加分/扣分弹窗
│   │   │   ├── ModalSettings.vue     # 设置与帮助弹窗
│   │   │   └── ModalShop.vue         # 小商店弹窗
│   │   ├── AppShell.vue        # 主应用外壳
│   │   ├── BatchBar.vue        # 批量评价工具栏
│   │   ├── LevelUpOverlay.vue  # 升级庆祝动画
│   │   ├── SortMenu.vue        # 排序菜单
│   │   ├── StudentCard.vue     # 学生卡片组件
│   │   └── StudentGrid.vue     # 学生网格布局
│   ├── lib/                    # 工具库
│   │   ├── autoBackup.ts       # 自动备份逻辑（File System Access API）
│   │   ├── idbKv.ts            # IndexedDB 封装
│   │   ├── models.ts           # 类型定义与工具函数
│   │   ├── pets.ts             # 宠物资源加载与管理
│   │   └── storage.ts          # localStorage 封装
│   ├── stores/                 # Pinia 状态管理
│   │   └── app.ts              # 主应用状态与业务逻辑
│   ├── App.vue                 # 根组件
│   ├── env.d.ts                # 环境类型声明
│   └── main.ts                 # 应用入口
├── docs/                       # 文档目录
│   ├── 班级宠物园_使用说明书.md
│   └── 班级宠物园_使用说明书.html
├── index.html                  # HTML 入口
├── package.json                # 项目配置与依赖
├── postcss.config.cjs          # PostCSS 配置
├── netlify.toml                # Netlify 部署配置
└── .gitignore                  # Git 忽略规则
```

---

## 三、核心文件详解

### 3.1 入口与配置

#### `index.html`
- HTML 页面入口，定义 `<div id="app"></div>` 挂载点
- 通过 `<script type="module" src="/src/main.ts">` 加载主模块

#### `package.json`
- 项目元信息与依赖管理
- 定义三个脚本：
  - `npm run dev` - 启动开发服务器
  - `npm run build` - 生产构建（包含 TypeScript 类型检查）
  - `npm run preview` - 预览构建结果

#### `src/main.ts`
- 应用入口文件
- 创建 Vue 应用实例并挂载 Pinia 状态管理
- 导入全局样式 `tailwind.css`

```typescript
createApp(App).use(createPinia()).mount('#app')
```

#### `src/App.vue`
- 根组件，仅包含 `<AppShell />` 一个子组件

---

### 3.2 核心组件

#### `src/components/AppShell.vue`
主应用外壳组件，包含：
- **顶部导航栏**：Logo、班级名称、设置按钮
- **功能区**：搜索框、排行榜、小商店、评价记录、撤回评价、批量评价
- **主内容区**：学生网格展示
- **弹窗路由**：根据 `app.ui.modal` 动态渲染对应弹窗
- **批量工具栏**：批量模式时显示

关键功能代码：
```vue
<!-- 撤回最近评价 -->
function undoLatest() {
  const res = app.undoLatestScoreRecord()
  if (!res.ok) window.alert(res.reason)
  else window.alert('已撤回最近一条评价')
}
```

#### `src/components/StudentCard.vue`
学生卡片组件，展示单个学生信息：
- **未领养状态**：显示蛋形图标 🥚 和"领养宠物"按钮
- **已领养状态**：
  - 宠物图片（10 个成长阶段）
  - 等级徽章（Lv.X）
  - 积分与徽章数
  - 成长进度条
- **交互功能**：
  - 点击卡片进入加分/扣分（已领养）或领养（未领养）
  - 批量模式下可勾选
  - 左上角"更换宠物"按钮

样式亮点：
- 宠物图片悬浮动画（`floaty` 关键帧）
- 加分/扣分时的动画效果（`bouncePlus` / `shakeMinus`）

#### `src/components/StudentGrid.vue`
学生网格布局组件：
- 响应式网格：单列/双列/三列自适应
- 空状态提示（无匹配学生时）

```vue
<div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
  <StudentCard v-for="s in students" :key="s.id" :student-id="s.id" />
</div>
```

#### `src/components/BatchBar.vue`
批量评价工具栏（固定在底部）：
- 显示已选学生数量
- "全选" / "清空" 快捷操作
- 批量评价按钮

#### `src/components/SortMenu.vue`
排序下拉菜单：
- 支持 4 种排序：姓名、学号、徽章、成长进度
- 点击外部自动关闭

#### `src/components/LevelUpOverlay.vue`
宠物升级全屏庆祝动画：
- 放射状背景
- 星星下落动画
- 宠物展示卡片
- 等级徽章展示

---

### 3.3 弹窗组件

#### `src/components/modals/ModalBase.vue`
弹窗基础容器组件：
- 半透明遮罩层
- 锁定背景滚动（`overflow: hidden`）
- 点击遮罩关闭
- 响应式尺寸

所有弹窗均继承此组件，统一交互体验。

#### `src/components/modals/ModalAdoptPet.vue`
领养宠物弹窗：
- 展示所有可选宠物（从 `assets/pets/` 自动扫描）
- 支持批量领养模式
- 更换宠物会清空积分（保证公平性）

#### `src/components/modals/ModalScore.vue`
加分/扣分弹窗：
- 按分类展示规则（学习/行为/健康/其他）
- 规则卡片显示分值（加分绿色、减分红色）
- 支持批量评价

#### `src/components/modals/ModalClassManager.vue`
班级与学生管理弹窗：
- **左侧边栏**：班级列表（新增/编辑/删除）
- **主内容区**：
  - 添加学生（单个/批量导入）
  - 学生列表（编辑/删除）
- 悬浮按钮设计（hover 显示编辑/删除）

#### `src/components/modals/ModalShop.vue`
小商店弹窗：
- **商品列表**：图标、名称、价格（徽章）、库存
- **兑换记录**：时间、学生、商品、花费、状态
- **管理商品**：新增/编辑/删除商品
- 内置图标选择器（31 个预设 emoji）

#### `src/components/modals/ModalLeaderboard.vue`
排行榜弹窗：
- 排序规则：徽章数 > 宠物等级 > 积分
- 展示 medals（👑🥈🥉）和排名
- 徽章说明文案

#### `src/components/modals/ModalRecords.vue`
评价记录弹窗：
- 展示最近 50 条评价
- 支持单条撤回（需有 `before` 状态）
- 导出 CSV（带 BOM 解决中文乱码）

```typescript
function exportExcel() {
  const csv = [header, ...rows].map((line) => line.map(escape).join(',')).join('\r\n')
  const bom = '\uFEFF'
  const blob = new Blob([bom, csv], { type: 'text/csv;charset=utf-8' })
  // ...
}
```

#### `src/components/modals/ModalSettings.vue`
设置与帮助弹窗，包含 4 个标签页：

**1. 加减分规则**
- 左侧：分类目录 + 统计
- 右侧：规则卡片（编辑/删除）
- 新增指标弹窗（图标/名称/分类/范围/分值）

**2. 成长设置**
- 配置 10 个等级的成长阈值
- 配置满级后徽章获取间隔

**3. 数据管理**
- JSON 导入/导出
- 自动备份设置（备份间隔、文件选择）

**4. 软件与帮助**
- 关于信息与反馈方式

---

### 3.4 工具库（lib/）

#### `src/lib/models.ts`
核心类型定义与工具函数：

```typescript
// 核心类型
export type ScoreRule = { /* 规则定义 */ }
export type PetState = { petId, name, level, exp }
export type Student = { id, name, number?, points, badges, pet? }
export type Classroom = { id, name, students: Student[] }
export type ScoreRecord = { /* 评价记录 */ }
export type ShopItem = { /* 商品定义 */ }
export type GrowthConfig = { thresholds, maxLevel, badgeEveryExp }

// 工具函数
export function newId(prefix: string): string  // 生成唯一 ID
export function expToNext(level: number): number  // 计算升级所需经验
```

#### `src/lib/pets.ts`
宠物资源管理：
- 使用 `import.meta.glob` 自动扫描 `assets/pets/` 下的图片
- 解析路径提取宠物名称和阶段
- 导出宠物列表和根据等级获取图片

```typescript
const petStageModules = import.meta.glob('@/assets/pets/*/*.{webp,png,jpg,jpeg,gif}', { eager: true })

export function petStageSrc(petId: PetId, level: number): string | undefined {
  const stage = Math.min(10, Math.max(1, Math.floor(level || 1)))
  return stageMap.get(petId)?.get(stage)
}
```

#### `src/lib/storage.ts`
localStorage 封装：
```typescript
export function loadJson<T>(key: string): T | null
export function saveJson(key: string, value: unknown)
```

#### `src/lib/idbKv.ts`
IndexedDB 封装（用于存储文件句柄等复杂对象）：
- `idbGet` / `idbSet` / `idbDel` 三个基础操作

#### `src/lib/autoBackup.ts`
自动备份功能（基于 File System Access API）：
- `getSupport()` - 检测浏览器支持
- `pickTargetFile()` - 选择备份文件
- `writeBackupJson()` - 写入备份

> 注意：仅 Chrome/Edge 等现代浏览器支持

---

### 3.5 状态管理（stores/）

#### `src/stores/app.ts`
**应用核心状态与业务逻辑**（约 670 行代码）

**State 结构**：
```typescript
state: {
  data: {
    version: 1,
    activeClassroomId: string,
    classrooms: Classroom[],
    rules: ScoreRule[],
    records: ScoreRecord[],
    shopItems: ShopItem[],
    shopRecords: ShopRecord[],
    growth: GrowthConfig,
  },
  ui: {
    query: string,           // 搜索关键词
    sortMode: SortMode,      // 排序方式
    modal: string | null,    // 当前弹窗
    batchMode: boolean,      // 批量模式
    autoBackupEnabled: boolean,  // 自动备份开关
    // ...
  }
}
```

**核心业务方法**：

| 方法 | 功能 |
|------|------|
| `applyRule(studentId, rule)` | 应用评分规则 |
| `undoScoreRecord(recordId)` | 撤回评价 |
| `adoptPet(studentId, petId, petName)` | 领养宠物 |
| `redeemShopItem(studentId, itemId)` | 兑换商品 |
| `upsertScoreRule(patch)` | 新增/编辑规则 |
| `upsertShopItem(patch)` | 新增/编辑商品 |
| `exportData()` / `importData(json)` | 数据导入导出 |
| `runAutoBackupOnce()` / `startAutoBackupLoop()` | 自动备份 |

**关键逻辑示例 - 应用评分规则**：
```typescript
applyRule(studentId: string, rule: ScoreRule) {
  const s = c.students.find(x => x.id === studentId)

  // 记录操作前状态（用于撤回）
  const before = {
    points: s.points,
    badges: s.badges,
    pet: s.pet ? { ...s.pet } : undefined,
  }

  // 更新积分
  s.points = Math.max(0, Math.floor(s.points + rule.delta))

  // 加分增加经验，扣分不影响经验
  if (rule.delta > 0 && s.pet) {
    s.pet.exp += rule.delta
    // 升级逻辑
    while (s.pet.level < maxLv && s.pet.exp >= this.expNeed(s.pet.level)) {
      s.pet.exp -= this.expNeed(s.pet.level)
      s.pet.level += 1
    }
    // 满级后获得徽章
    while (s.pet.level >= maxLv && s.pet.exp >= this.expNeed(maxLv)) {
      s.pet.exp -= this.expNeed(maxLv)
      s.badges += 1
    }
  }

  // 记录评价
  this.data.records.unshift({ ... })
  this.persist()
}
```

---

### 3.6 样式文件

#### `src/assets/tailwind.css`
Tailwind CSS 入口：
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  color-scheme: light;
}
```

#### 样式约定
组件使用 `<style scoped>` 隔离作用域，通过 `@apply` 组合 Tailwind 工具类：
```vue
<style scoped>
.btn {
  @apply rounded-2xl px-4 py-2 text-sm border border-slate-200 bg-white hover:bg-slate-50 transition;
}
.btn-primary {
  @apply rounded-2xl px-4 py-2 text-sm bg-brand-500 text-white hover:bg-brand-600 transition shadow-soft;
}
</style>
```

---

## 四、数据流与状态管理

### 4.1 数据持久化
所有数据保存在浏览器 `localStorage`：
- Key: `class-pet-home:data`
- 自动备份时使用 File System Access API 写入本地文件

### 4.2 状态更新流程
```
用户操作 → 组件调用 store 方法 → 更新 state → persist() 保存 → 响应式更新 UI
```

### 4.3 宠物成长逻辑
```
加分 → 增加经验 → 检查是否达到升级阈值
     → 升级（Lv.1→Lv.2→...→Lv.10）
     → 满级后继续积累 → 每 200 经验 = 1 枚徽章
```

---

## 五、构建与部署

### 5.1 开发
```bash
npm install
npm run dev
```

### 5.2 生产构建
```bash
npm run build
# 输出到 dist/ 目录
```

### 5.3 本地预览
```bash
npm run preview -- --host 0.0.0.0 --port 4173
```

### 5.4 部署配置
- `netlify.toml` - Netlify 部署配置
- `index.html` - 单页应用入口

---

## 六、特色功能实现

### 6.1 自动备份
基于浏览器的 File System Access API：
1. 用户选择备份文件（需授权）
2. 将文件句柄存入 IndexedDB
3. 定时/页面隐藏时触发备份

### 6.2 宠物图片动态加载
使用 Vite 的 `import.meta.glob`：
```typescript
const petStageModules = import.meta.glob('@/assets/pets/*/*.{webp,png,jpg,jpeg,gif}', { eager: true })
```
自动扫描 `assets/pets/` 下所有图片，按文件夹名和文件名解析宠物信息与阶段。

### 6.3 评价撤回
每条记录保存操作前状态 `before`：
```typescript
before?: {
  points: number
  badges: number
  pet?: PetState
}
```
撤回时直接恢复 `before` 状态。

### 6.4 批量评价
1. 进入批量模式 → 显示底部工具栏
2. 勾选符合条件的学生（领养/未领养）
3. 统一执行评价/领养操作

---

## 七、总结

本项目是一个**功能完整、代码规范良好**的 Vue 3 单页应用，体现了以下特点：

1. **清晰的分层架构**：Components / Stores / Lib 职责分明
2. **良好的 TypeScript 支持**：完整的类型定义
3. **响应式设计**：Tailwind CSS + 响应式布局
4. **用户体验优秀**：动画效果、悬浮交互、空状态提示
5. **数据安全意识**：localStorage + 自动备份双保险
6. **可维护性强**：组件拆分合理、代码注释清晰

项目适用于课堂管理场景，无需后端即可独立运行，也可扩展云同步功能。

---

**作者信息**
- 作者：duan cong
- 项目版本：0.1.0

